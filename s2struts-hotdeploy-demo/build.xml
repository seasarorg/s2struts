<project name="S2StrutsHotdeployDemo" default="dist" basedir=".">

  <property file="project.properties"/>

  <property name="releaseFile" value="S2StrutsHotdeployDemo-V${version}.zip" />
  
  <property name="projectDir" value="s2struts-hotdeploy-demo" />

  <target name="dist">
    <zip destfile="../${releaseFile}"
         basedir=".." includes="${projectDir} , ${projectDir}/**"
         excludes="**/.DS_Store , ${projectDir}/work/**"/>
  </target>

</project>
